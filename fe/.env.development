# Docker dev / remote browser: leave REACT_APP_API_URL unset — apiBaseUrl.js auto-detects.
# Local `npm start` on your laptop: create fe/.env.development.local with:
#   REACT_APP_API_URL=http://localhost:8000/api
