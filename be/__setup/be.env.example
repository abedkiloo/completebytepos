# Copy to be/.env for local manage.py (outside Docker), or use workspace root .env
#   cp .env.example .env

DEBUG=True
SECRET_KEY=change-me-to-a-long-random-string-in-production
DJANGO_SETTINGS_MODULE=config.settings

POSTGRES_DB=completebytepos
POSTGRES_USER=completebytepos
POSTGRES_PASSWORD=completebytepos

# Local Postgres on host (not Docker service name)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=completebytepos
DB_USER=completebytepos
DB_PASSWORD=completebytepos
